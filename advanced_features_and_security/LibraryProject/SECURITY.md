# SECURITY SETTINGS:
# - DEBUG = False disables detailed error pages in production
# - SECURE_BROWSER_XSS_FILTER enables browser XSS protection
# - CSRF_COOKIE_SECURE ensures CSRF tokens are sent over HTTPS
# - CSP headers restrict content sources to prevent injection